fixtures:
    symlinks:
      vmbuilder: "#{source_dir}"
    repositories:
      stdlib: https://github.com/puppetlabs/puppetlabs-stdlib.git
      inifile: https://github.com/puppetlabs/puppetlabs-inifile.git
    forge_modules:
